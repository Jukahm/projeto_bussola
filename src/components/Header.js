import React from 'react';
import logo from '../img/spot_icon.png';

export const Header = () => {
    return(
        <header>
            <img src={logo} alt="PopSearch_Logo"/>
        </header>
    );
};