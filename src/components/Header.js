import React from 'react';
import logo from '../img/spot_icon.png';

export const Header = () => {
    return(
        <header>
            <div>
                <img src={logo}/>
            </div>
        </header>
    );
};